export type TDataStatuses = 'initial' | 'success' | 'error' | 'fetching' | 'updating';

export enum dataStatuses {
    initial = 'initial',
    success = 'success',
    error = 'error',
    fetching = 'fetching',
    updating = 'updating'
}
